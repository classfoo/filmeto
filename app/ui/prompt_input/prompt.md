重新设计实现prompt_input组件，进行如下的功能实现：
1，prompt输入组件分为左边配置部分和右边prompt输入部分
2，左边配置部分通过选中的tool进行切换展示，通过plugin/tools中各个tool的init_ui方法来初始化控件和行为
3，右边prompt输入框占满剩余空间
4，右边prompt输入框的最右边浮现发送按钮，发送按钮上呈现prompt的token数
5，整个组件需要能够占满父容器宽度和高度
6，保持现有功能不变